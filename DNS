
1- mon DNS contacte le forwarder (généralement 8.8.8.8) et il retourne la réponse "rapide"
2- Sinon, le serveur fait des requêtes récursives depuis la racine . "root hints" puis le TLD .fr puis google.fr pour pouvoir retrouver l'ip de www.google.fr  "plus lent"
           il existe 13 root hints sur internet

Get-WinEvent -LogName "DNS Server"
Get-WinEvent -LogName "DNS Server" | where LevelDisplayName -eq "Error"

nltest /dclist:castel.afrique
dcdiag /e /v /c /f:dcdiag_report.txt
repadmin /replsummary
repadmin /showrepl

Vérifier un seul serveur AD : 
 dcdiag /v 
 dcdiag /test:dns /v
 dfsrdiag replicationstate

vérifier la NTDS : 
esentutl /g C:\Windows\NTDS\ntds.dit

le Dynamic DNS permet aux serveurs AD de mettre à jour les enregistrements DNS des machines jointes au domaine de façon dynamique

======================================

https://mxtoolbox.com/

Chaque domaine a plusieurs info à récupérer : MX Lookup, DNS lookup, SPF record, 
                 
exemple de castel-afrique.com (SPF record lookup)

Statut SPF : 
v=spf1 a mx ip4:185.74.203.12 ip4:40.89.165.135 ip4:169.239.99.62 ip4:185.74.203.12 ip4:41.159.13.115 ip4:65.181.122.52 ip4:65.181.127.140 ip4:102.68.63.66 include:spf.protection.outlook.com include:_spf.general.transactional-mail-a.com -all

Les serveurs MAIL/EDGE qui sortent sur internet avec ces IP publiques utilisent le SPF castel-afrique.com
Ces serveurs doit être configurés à envoyer vers le smarthost "castelafrique-com01e.mail.protection.outlook.com", à voir sur get-sendconnector de chaque serveur mail



je dois chercher par mon IP publique pour vérifier s'elle est déclarée sur le SPF d'un domaine X, si j'héberge un serveur mail, mon IP publique devrait être déclarée uniquement sur mon SPF.


PTR : DNS inverse (1.2.3.4 -> mail.exemple.com), Aider les serveurs de réception à vérifier que l’IP d’envoi correspond bien à un domaine connu (utile contre les spams).

      Les messages provenant d’IP sans enregistrement PTR sont souvent considérés comme suspects (même dans un domaine active directory, un serveur exchange doit avoir un PTR record)

MX : Indique quel serveur gère les e-mails pour un domaine (précise le serveur qui héberge les BAL d'un domaine)
                Quels sont les MX de exemple.com ?
                réponse MX : exemple.com   MX   10 mail.exemple.com

SPF : Qui est autorisé à envoyer des mails via mon domaine
      Enregistrement TXT dans le DNS qui liste les IP publiques autorisées à envoyer des emails avec le nom de mon domaine (anti-spoofing)

DKIM : signature des emails 
      clé publique dans un enregistrement TXT DNS pour signer numériquement les e-mails, le serveurs destination vérifie que le mail n'a pas été modifié

DMARC : Politique en cas d’échec SPF/DKIM 
       politique DNS qui indique aux serveurs de réception quoi faire si SPF et/ou DKIM échouent, --> mise en quarantaine.. 

TXT : Conteneur de données (SPF, DKIM, DMARC, autres infos) pour ce qui est déjà cité dessus.
